These schema files were reverse engineered from the various tools' output,
using XML Spy (DTD/Schema --> Generate DTD/Schema --> W3C Schema).

They were then tweaked to change types, for example changing line numbers from
enumerations to integers.